---
name: granola-cli
description: Use granola-cli to read Granola folders, notes, summaries, and transcripts through the official Public API.
allowed-tools: Bash(granola-cli:*)
hidden: true
---

# granola-cli

Install:

```bash
npm install -g git+https://github.com/toolittlecakes/granola-cli.git
```

Load the version-matched guide:

```bash
granola-cli skill
```
